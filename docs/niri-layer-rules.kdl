// Live wallpaper in niri's backdrop (no extra nwall-backdrop layer); Overview uses smart-pause.
layer-rule {
    match namespace="^nwall-live$"
    place-within-backdrop true
}

layout {
    background-color "transparent"
}
